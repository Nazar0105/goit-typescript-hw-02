// У TypeScript для збереження гнучкості типу можна використовувати тип any. Отже, тип для змінної anything може бути визначений як any:

let anything: any = -20;
anything = 'Text';
anything = {};

// Змінна "anything" з типом "any" може зберігати значення будь-якого типу без перевірок типів під час компіляції.